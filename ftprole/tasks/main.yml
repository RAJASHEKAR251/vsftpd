---
- name: Install Vsftpd
  apt:
      name: vsftpd
      state: present

   
  template:
      src: templates/vsftpdconfig.j2
      dest: /etc/ansible.conf

  
  sysctl:
      name: vsftpd
      state: present
      

