---
  - name: install vsftpd
    apt:
     name: vsftpd
     state: present

   
    template:
      src: templates/vsftpdconfig.j2
      dest: /etc/ansible.conf

    user:
     name: devops
     password: devopsftp  

  
    service:
     name: vsftpd
     state: started
     enabled: yes

