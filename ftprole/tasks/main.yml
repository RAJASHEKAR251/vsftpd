---
- name: Install Vsftpd
  apt:
      name: vsftpd
      state: present

   
  copy:
      src: templates/vsftpdconfig.j2
      dest: /etc/ansible.conf

  
  sysctl:
      name: vsftpd
      state: present
      

